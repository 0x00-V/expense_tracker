# Expense Tracker CLI

A command-line Expense Tracker built in Python.  
Track, view, and visualize your spending with a clean interactive interface — all stored locally in an SQLite database.

---

## Requirements

- Python 3.10 or newer  
- Dependencies listed in `requirements.txt`

Install them with:
```bash
pip install -r requirements.txt
```
